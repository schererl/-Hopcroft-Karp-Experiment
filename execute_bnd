#!/bin/bash

# Specify the file name or accept it as a command-line argument
filename="$1"

# Check if the file name is provided
if [[ -z "$filename" ]]; then
    echo "Usage: $0 <filename>"
    exit 1
fi

g++ graph.cpp BND.cpp main.cpp -o max_matching
zcat $filename | ./max_matching 1